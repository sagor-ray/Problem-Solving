#include <stdio.h>
#include <math.h>
int main()
{
    int n,i;
    printf("Enter the value of n: ");
    scanf("%d",&n);
    for(i=0;i>=n;i++)
    {
        printf("%d",i);
    }
    return 0;
}
